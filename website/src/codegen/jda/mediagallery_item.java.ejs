<%
const args = [];
if (data.comp.description) args.push(`description: "${data.comp.description}"`);
if (data.comp.spoiler) args.push(`spoiler: true`);
%>MediaGalleryItem.fromUrl(<%= data.comp.media.url %><% if (args.length) { %>, { <%- args.join(', ') %> }<% } %>)
